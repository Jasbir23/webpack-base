const {
    innerHeight: h,
    innerWidth: w
} = window;
export const
    defaultCategory = 0x0001,
    redCategory = 0x0002,
    BLOCK_WIDTH = 0.1 * w,
    ANGLE_CHANGE_CONST = 0.05,
    CAR_WIDTH = 0.02 * w,
    CAR_HEIGHT = 0.08 * h,
    FORCE_CONST = 0.0000002 * w;